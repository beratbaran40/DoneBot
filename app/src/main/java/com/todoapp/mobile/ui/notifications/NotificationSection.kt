package com.todoapp.mobile.ui.notifications

/** Which date bucket a notification is rendered under — drives the card's timestamp format. */
internal enum class NotificationSection { TODAY, YESTERDAY, EARLIER }
