*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Thesis sources (local only)
thesis/

# Claude-generated docs & tooling (local only)
CLAUDE.md
DONEBOT_CAPABILITIES.md
# Deliberately unanchored: the report lives in a dated subfolder, not at the root.
ARASTIRMA_RAPORU.md
# Design-skill working docs (brief / IA / tokens / tasks / plans)
.design/
.claude/commands/
.claude/skills/
.claude/scheduled_tasks.lock
.claude/workflows/

# Local Claude-generated plan docs (<FEATURE>_PLAN.md). Kept for future ones — matches nothing today.
*_PLAN.md

# Screenshots & local docs (not part of source)
docs/
thesis-screenshots/
.screenshots-review/
# Prod-readiness working docs — local-only, never tracked (report, session notes, screenshots)
/donebot prod/

# Secrets, signing & environment (never commit)
# Environment files
.env
.env.*
*.env
# Signing keystores & configs
*.jks
*.keystore
keystore.properties
signing.properties
*.p12
*.pfx
# Certificates & private keys
*.pem
*.cer
*.der
*.key
# Service-account / credential JSON & secret properties
*-service-account*.json
*service_account*.json
*-credentials.json
credentials.json
secrets.properties
secrets.xml

# Session reports / scratch docs (local-only)
SESSION_*.md
BUG_HUNT_*.md
# Dated release-prep runbooks (RELEASE_<yyyy-mm-dd>.md). The `20` keeps this from
# swallowing a RELEASE_NOTES.md, which would belong in the repo.
RELEASE_20*.md

# App Screenshots — loose root exports + Play Store source sets (local only).
# Root-anchored on purpose: unanchored names would also swallow the tracked
# README copies under .github/assets/ (core.ignorecase matches ChatBot.png ⇄ chatbot.png).
/Activity.png
/Calender.png
/Chatbot.png
/DoneBot Feature Graphic.png
/Groups.png
/Home - Dark.png
/Home- Light.png
/Onboarding.png
/Pomodoro.png
/Donebot Feature Graphic TR.png
/DoneBot Play Store Uygulama Görselleri/
/DoneBot Play Store Uygulama Görselleri TR/
/app/release/app-release.aab
# Unprocessed Pomodoro ambience sources. The whole folder, not the three files that
# happen to be in it today: tools/prep_ambience.sh turns these into the loops under
# app/src/main/res/raw/ (which ARE tracked — they ship), so the originals only need to
# exist on the machine that regenerates them.
/pomodoro_background_noises/